This file lists all the libraries needed to run your project.

numpy
matplotlib

💡 Explanation:
NumPy → used for matrix/grid creation (your 20×20 environment)
Matplotlib → used to visualize the path taken by the agent

👉 On GitHub, this helps others install dependencies easily using:
pip install -r requirements.txt
